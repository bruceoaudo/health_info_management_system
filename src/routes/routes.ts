import { Router } from "express";

const router = Router()

router.post("/create-program", )
router.post("/register-client", )
router.post("/enroll-client-to-program", )
router.get("/get-all-clients", )
router.post("/search-for-client", )
router.get("/client-profile", )


export default router;